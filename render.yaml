services:
  - type: web
    name: internet-search-mcp
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: python search_mcp.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
